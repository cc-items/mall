const state = function () {
  return {};
};

const mutations = {};

const actions = {};

const getters = {};

const modules = {};
export default {
  // 防止状态污染
  state,
  //默认是全局的
  mutations,
  actions,
  getters,
  modules
};
